# Production models

Place optimized GLB files here. Requirements and filenames are documented in
`assets/ASSET-BRIEF.md`.

The current app uses procedural stand-ins until `modelSrc` is set for a stop in
`src/stops-config.js`. The magic bus stop already loads `magic_bus.glb`.
